if has nix; then
  local supports_flakes=$(nix show-config | grep experimental-features | grep flakes 2>&1 > /dev/null)

  if $supports_flakes; then
    use flake
  else
    use nix
  fi
fi

layout ruby
